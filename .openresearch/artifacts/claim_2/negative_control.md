# Claim 2 negative control

With `c(e)=e`, the lower-capability player can reduce effort from `3/4` to
`51/100`, remain strictly ahead, and increase utility from `1/4` to `49/100`.
Thus the counterexample fails when the flat-cost mechanism is removed.
