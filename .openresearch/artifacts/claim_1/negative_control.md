# Claim 1 negative control

With the reward gap reduced from one to `1/4`, below the `1/2` catch-up effort
under linear cost, the all-zero profile is a PNE. The checker requires the
symbolic “profitable deviation exists” formula to be unsatisfiable.
