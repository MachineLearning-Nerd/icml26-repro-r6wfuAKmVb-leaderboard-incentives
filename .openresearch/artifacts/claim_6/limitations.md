# Claim 6 limitations and deviations

No Qwen model is fine-tuned in this route. That is a material deviation and is
why the verdict is not VERIFIED. Full fine-tuning of five models through 3,000
steps is also outside the authorized CPU-only campaign. The exact unblocker is
the author measurements and fitting configuration, or published CPU-feasible
logits/checkpoints sufficient to recreate them.
