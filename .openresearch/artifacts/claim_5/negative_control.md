# Claim 5 negative control

With common `L=0,U=1`, `alpha_high=log(2)>alpha_low=0`, and equal beta one,
the catch-up infimum becomes `1+Delta`. Reward gap two is therefore stabilized
at `Delta=1`. This removes the model-specific-bound cancellation responsible
for the counterexample.
