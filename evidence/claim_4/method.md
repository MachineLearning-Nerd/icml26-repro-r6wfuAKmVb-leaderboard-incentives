# Claim 4 method

Common TbT adds the same `Delta` inside `theta+Delta+e`, so it cancels in every
ranking comparison. Under the flat admitted cost and reward gap `1/10`,
profiles `[1,0]` and `[1,1]` are both PNE for every `Delta>=0`; all zero admits
the profitable lower-player effort `3/4`.
